**BACKEND**

1. Backend Framework
2. OCR & Computer Vision
3. Deep Learning / Object Detection
4. Data Processing
- FastAPI + Uvicorn **→** API service.
- YOLO (Ultralytics) + PyTorch **→** Object detection (kotak field).
- EasyOCR + Tesseract **→** OCR baca teks.
- OpenCV + NumPy + Regex **→** Preprocessing gambar + ekstraksi teks.
- CORS, python-multipart, Pillow **→** utilitas API & file handling.

**FRONTEND**

- React.js
- TypeScript
- React Image Crop
- HTML5 Media API (Camera)
- HTML5 Canvas API
- CSS
